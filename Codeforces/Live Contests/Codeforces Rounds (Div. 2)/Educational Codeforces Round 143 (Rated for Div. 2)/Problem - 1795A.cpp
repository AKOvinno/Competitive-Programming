#include<bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while(t--) {
        int n, m;
        cin >> n >> m;
        string tower_one, tower_two;
        cin >> tower_one >> tower_two;

    }
    return 0;
}
